{
  "name": "skills",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish",
    "build": "bun run --filter='agent-worker' build"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0"
  }
}
